node_modules/
dist/
# Private signing keys never belong in this repository.
*.key.json
*.pem
*.private.*
# Vercel CLI link and its short-lived credentials (vercel link writes .env.local).
.vercel/
.env*
